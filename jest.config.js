/* eslint-disable no-undef */
module.exports = {
    rootDir:"./src",
    preset: "ts-jest",
    setupFiles: ["dotenv/config"],
    //  testEnvironment: "node", // uncomment this and jest will fail with "referencerror on describe"
  };
